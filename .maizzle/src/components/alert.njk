<div class="p-2">
  <div class="inline-flex items-center bg-white leading-none text-{{ data.textColor or 'gray-600' }} rounded-full p-2 shadow text-sm">
    <span class="inline-flex bg-{{ data.color or 'blue-600' }} text-white rounded-full h-6 px-3 justify-center items-center">{{ data.value or 0 }} {{ data.name or 'Success' }}</span>
    <span class="inline-flex px-2">{{ data.text or 'Text content goes here.' }}</span>
  </div>
</div>
